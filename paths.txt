# Djowda Broadcaster Paths Configuration
# Set SHOULD_SYNC=true to trigger broadcast, then commit and push
# After broadcast completes, set it back to false

SHOULD_SYNC=true

# Paths to broadcast (directories should end with /)


.github/FUNDING.yml
.github/deployment.yml
fastlane/
Gemfile
repos.txt
Shared_res
DjowdaMap

